import { ColorSchemes } from "../../types/theme";
type propType = {
    type?: ColorSchemes;
};
export declare const useStyles: (props?: propType) => {
    container: object;
    item: object;
    active: object;
    dropdown: object;
};
export {};
