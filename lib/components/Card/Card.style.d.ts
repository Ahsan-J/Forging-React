import { Size } from "../../types/theme";
type propType = {
    size?: Size;
    bgAlpha?: number;
};
export declare const useStyles: (props?: propType) => {
    container: object;
    body: object;
    header: object;
    footer: object;
};
export {};
