import { Size } from "../../types/theme";
type propType = {
    size?: Size;
};
export declare const useStyles: (props?: propType) => {
    container: object;
    dropdownContent: object;
    show: object;
};
export {};
