import { Size } from "../../types/theme";
type propType = {
    bgAlpha?: number;
    size?: Size;
};
export declare const useStyles: (props?: propType) => {
    container: object;
    fileItem: object;
    deleteIcon: object;
    errorFeedback: object;
};
export {};
