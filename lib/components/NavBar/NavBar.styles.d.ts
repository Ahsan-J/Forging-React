import { ColorSchemes } from '../../types/theme';
type propType = {
    type?: ColorSchemes;
    theme?: string;
};
export declare const useStyles: (props?: propType) => {
    container: object;
};
export {};
