import { ColorSchemes } from '../../types/theme';
type propType = {
    type?: ColorSchemes;
    bgAlpha?: number;
};
export declare const useStyles: (props?: propType) => {
    container: object;
    previewContainer: object;
    topContainer: object;
    video: object;
    iconContainer: object;
    controllerContainer: object;
    slider: object;
    controllers: object;
    durationWrapper: object;
    timerStyle: object;
    volumeContainer: object;
    volumeSlider: object;
};
export {};
