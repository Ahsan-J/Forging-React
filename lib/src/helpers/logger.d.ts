export declare const Logger: {
    log(key: string | TemplateStringsArray, ...data: unknown[]): void;
    error(key: string | TemplateStringsArray, ...data: unknown[]): void;
    warn(key: string | TemplateStringsArray, ...data: unknown[]): void;
};
